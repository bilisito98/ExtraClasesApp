@model ExtraClasesApp.Models.ErrorViewModel

@{
    ViewData["Title"] = "Error";
}

<div class="container mt-5">
    <h1 class="text-danger">Ha ocurrido un error.</h1>
    <h4 class="text-danger">No se pudo completar la solicitud.</h4>

    @if (Model?.ShowRequestId == true)
    {
        <div class="mt-3">
            <p><strong>ID de solicitud:</strong> <code>@Model.RequestId</code></p>
        </div>
    }

    <hr />
    <h5>Modo Desarrollo</h5>
    <p>
        Si estás desarrollando la aplicación, cambia al entorno <strong>Development</strong> para ver más detalles del error.
    </p>
    <p class="text-warning">
        <strong>Advertencia:</strong> No habilites el entorno de desarrollo en aplicaciones en producción, ya que puede exponer información sensible.
    </p>
    <p>
        Para habilitar el entorno de desarrollo, establece la variable de entorno <code>ASPNETCORE_ENVIRONMENT</code> a <strong>Development</strong> y reinicia la aplicación.
    </p>
</div>
